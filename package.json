{
  "name": "hardhat-project",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "dotenv": "^16.0.2",
    "hardhat": "^2.10.2"
  },
  "dependencies": {
    "@ensdomains/ens-contracts": "^0.0.12",
    "@nomicfoundation/hardhat-chai-matchers": "^1.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "@openzeppelin/contracts-upgradeable": "^4.7.3",
    "@typechain/ethers-v5": "^10.1.0",
    "@typechain/hardhat": "^6.1.2",
    "@types/chai": "^4.2.0",
    "@types/mocha": "^9.1.0",
    "chai": "^4.2.0",
    "ethers": "^5.4.7",
    "hardhat-gas-reporter": "^1.0.8",
    "solidity-coverage": "^0.7.21",
    "ts-node": ">=8.0.0",
    "typechain": "^8.1.0",
    "typescript": ">=4.5.0"
  },
  "scripts": {
    "compile": "yarn hardhat compile",
    "start:node": "yarn hardhat node",
    "test": "yarn hardhat test",
    "deploy:local": "yarn hardhat run --network localhost scripts/deploy.js",
    "deploy:goerli": "yarn hardhat run --network goerli scripts/deploy.js",
    "deploy:mainnet": "yarn hardhat run --network mainnet scripts/deploy.js",
    "deploy:mainnet_fork": "yarn hardhat run --network mainnet_fork scripts/deploy.js",
    "dryRun:goerli": "yarn hardhat run --network goerli scripts/dryRun.js",
    "dryRun:mainnet": "yarn hardhat run --network mainnet scripts/dryRun.js",
    "dryRun:mainnet_fork": "yarn hardhat run --network mainnet_fork scripts/dryRun.js"
  }
}
